import { SignedinDirective } from './signedin.directive';

describe('SignedinDirective', () => {
  it('should create an instance', () => {
    const directive = new SignedinDirective();
    expect(directive).toBeTruthy();
  });
});
